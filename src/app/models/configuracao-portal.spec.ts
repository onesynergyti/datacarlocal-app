import { ConfiguracaoPortal } from './configuracao-portal';

describe('ConfiguracaoPortal', () => {
  it('should create an instance', () => {
    expect(new ConfiguracaoPortal()).toBeTruthy();
  });
});
