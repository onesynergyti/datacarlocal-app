import { ConfiguracaoMensagens } from './configuracao-mensagens';

describe('ConfiguracaoMensagens', () => {
  it('should create an instance', () => {
    expect(new ConfiguracaoMensagens()).toBeTruthy();
  });
});
