import { ConfiguracaoRecibo } from './configuracao-recibo';

describe('ConfiguracaoRecibo', () => {
  it('should create an instance', () => {
    expect(new ConfiguracaoRecibo()).toBeTruthy();
  });
});
